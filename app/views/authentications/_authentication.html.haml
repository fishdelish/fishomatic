%li.authentication
  .image= image_tag(authentication.provider.to_s.downcase + ".png", :size => "32x32")
  .title= authentication.provider.humanize
  .remove= link_to "X", authentication_path(authentication), :method => :delete
